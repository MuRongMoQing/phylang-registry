# GitHub 部署架构与边界

PhyLang 注册表由静态 `index.json`、`.phypkg` 文件、Schema 和健康文件组成，不需要数据库或常驻服务。GitHub Actions 构建静态目录，GitHub Pages 发布，完整性工作流随后从公开 URL 重新读取并验证。

0.6.2 部署模板不包含 Dependency Review、默认 Dependabot 和 Artifact Attestation，避免与该仓库实际依赖结构无关的 API、权限和账户功能差异。
