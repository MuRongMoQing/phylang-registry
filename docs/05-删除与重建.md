# 删除与重建

删除远程仓库是不可逆的破坏性操作。确认仓库名后执行：

```powershell
gh auth refresh -h github.com -s delete_repo
gh repo delete MuRongMoQing/phylang-registry --yes
```

删除本地目录前先切换到上一级：

```powershell
Set-Location ..
Remove-Item -LiteralPath <部署目录> -Recurse -Force
```

重新部署时使用全新解压目录，不要混用旧 `.git`、旧工作流或旧修复脚本。
