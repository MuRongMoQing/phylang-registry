# 工作流说明

- `validate.yml`：Go test/vet、所有包 validate/audit/lock/test、注册表 dry-run。
- `deploy.yml`：构建 Pages 目录并使用官方 Pages Actions 发布。
- `integrity.yml`：部署成功后重试公开 URL，并用 PhyLang 客户端检查索引。
- `release.yml`：仅在标签或手动运行时创建 GitHub Release。

每个 `.sh` 都由 `bash ./scripts/...` 显式调用；bootstrap 同时把脚本模式写为 `100755`。
