# 权限与安全

验证和完整性检查只需要 `contents: read`。Pages 的 deploy Job 单独获得 `pages: write` 与 `id-token: write`。Release 工作流单独获得 `contents: write`。bootstrap 将仓库默认 `GITHUB_TOKEN` 权限设置为只读，并禁止工作流批准 Pull Request。

社区包运行时不提供任意文件、进程或网络调用；下载的包由注册表 SHA-256 校验。
